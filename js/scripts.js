$(document).ready(function() {

  $(".card").click(function() {
    if
    ($(".front").hasClass("flip")) {
      $(".front").hide();
      $(".back").show();
    }
      else {
        $(".front").addClass("flip");
      }
  });






});
